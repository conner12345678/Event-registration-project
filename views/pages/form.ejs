<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../event.css">
    <title>Edit Event</title>
</head>
<body>
    <h1>Edit Event</h1>
    <form action="/events/<%= event.id %>" method="POST">
        <p name="event"><%= event.event %></p>
        <p name="dayOne"><%= event.dayOne %>-<%= event.dayTwo %></p>
        <p name="location"><%= event.location %></p>
        <p name="address"><%= event.address %></p>
        <textarea cols="40" rows="10" name="names" required><%= event.names %></textarea>
        <button type="submit">Update Event</button>
    </form>
    <a href="/">Back to list</a>
    <button type="button" class="back"><a href="/">Back to list</a></button>
</body>
</html>